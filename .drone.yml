kind: pipeline
type: docker
name: minimal-arm-build

steps:
  - name: build
    image: alpine:latest
    commands:
      - apk update 
      - apk add --no-cache cmake make git gcc-arm-none-eabi
      - mkdir build && cd build
      - cmake .. -DCMAKE_TOOLCHAIN_FILE=../toolchain.cmake
      - make