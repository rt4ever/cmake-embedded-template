kind: pipeline
type: docker
name: minimal-arm-build

steps:
  - name: build
    image: alpine:latest
    commands:
      - apk add --no-cache cmake make git arm-none-eabi-gcc
      - mkdir build && cd build
      - cmake -G "Ninja"
      - ninja
      
trigger:
  event:
    - pull_request
